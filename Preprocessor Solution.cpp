/* Enter your macros here */
#define INF 0
#define toStr(x) #x
#define io(s) cin>>s
#define foreach(list, index) for(unsigned int i=0;i<list.size();i++)
#define FUNCTION(name, op) void name(int &x, int y) {if(!(x op y)) x=y;}